include icon

mixin button(props)
  -
    const {
      text,
      type = 'primary',
      size = null,
      icon = null,
      type = null,
    } = props;

  button.btn(
    class=`btn-${type}`
    class=size ? `btn-${size}` : null
    class=icon ? 'btn-icon' : null
    class!=attributes.class
    type=type
  )
    span.btn-text= text
    if icon
      +icon({ name: icon, size: 'md' })
